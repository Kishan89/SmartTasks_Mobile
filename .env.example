# API Mode: 'simulated' or 'real'
API_MODE=simulated

# If using real backend, set the base URL
# API_BASE_URL=https://api.example.com

# Simulated server settings (only used when API_MODE=simulated)
SIMULATED_LATENCY_MS=500
SIMULATED_FAILURE_RATE=0.2
